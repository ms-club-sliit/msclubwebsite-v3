// This file is for testing husky and lint-staged. It will be deleted later.

function add(a, b) {
  return a + b;
}

const user = {
  name: "Alice",
  age: 30,
};

function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet(user.name);
